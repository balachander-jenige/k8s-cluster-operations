apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: tracedata
    type: backend
spec:
  selector:
    app: tracedata
  type: NodePort
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30008



